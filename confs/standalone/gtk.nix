{ pkgs, ... }:

{
	# programs.chromium.enable = true;

	# home = {
	# 	packages = with pkgs; [ dconf ];
	# };

	# dconf = {
	# 	enable = true;
	# };

	# gtk = {
	# 	enable = true;
	# };

	# qt = {
	# 	enable = true;
	# };
}
